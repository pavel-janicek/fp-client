Login reliability (release 1.4.0):
- Fix: the app no longer gets stuck on "Unauthorized" errors — after the server tightened its login-token rules, the app now returns to the sign-in screen automatically and a single re-login restores everything.
- The same fix also covers expired sessions and instances that rotated their secret keys.